<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_edit_post"
        android:title="@string/community_action_edit"
        android:visible="false"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_delete_post"
        android:title="@string/community_action_delete"
        android:visible="false"
        app:showAsAction="never" />

    <item
        android:id="@+id/action_report_post"
        android:title="@string/community_action_report"
        android:visible="false"
        app:showAsAction="never" />
</menu>
